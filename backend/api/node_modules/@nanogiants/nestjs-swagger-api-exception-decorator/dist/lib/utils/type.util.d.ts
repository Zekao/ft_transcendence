import { MergedOptions, Template } from '../interfaces/options.interface';
export declare const buildMessageByType: (template: Template, options: MergedOptions) => void;
export declare const resolveTypeTemplate: (options: MergedOptions) => void;
